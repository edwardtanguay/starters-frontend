export interface IStarter {
	id: number;
	idCode: string;
	title: string;
	description: string;
	featureList: string;
	features: string[];
	githubUrl: string;
	imageUrl: string;
	readmeText: string;
	installLines: string[];
	isFullStack: boolean;
	animationUrl: string;
}